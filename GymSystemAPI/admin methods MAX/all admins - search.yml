info:
  name: all admins / search
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/admin"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  params:
    - name: name
      value: Fran
      type: query
      disabled: true
    - name: limit
      value: "2"
      type: query
      disabled: true
    - name: next_cursor
      value: admin1@example.com
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
