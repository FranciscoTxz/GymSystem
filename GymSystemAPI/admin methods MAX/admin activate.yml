info:
  name: admin activate
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{URL}}/admin/activate/admin1@example.com"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
