info:
  name: admin Info specific
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/admin/info/admin1@example.com"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
