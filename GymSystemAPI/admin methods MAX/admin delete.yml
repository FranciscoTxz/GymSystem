info:
  name: admin delete
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{URL}}/admin/delete/admin2@example.com"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
