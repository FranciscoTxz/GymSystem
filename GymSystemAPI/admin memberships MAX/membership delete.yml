info:
  name: membership delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{URL}}/membership/info/fullx"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
