info:
  name: deactivate user
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{URL}}/user/deactivate/1"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
