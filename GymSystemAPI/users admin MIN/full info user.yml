info:
  name: full info user
  type: http
  seq: 5

http:
  method: GET
  url: "{{URL}}/user/info/1/full"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
