info:
  name: delete user
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{URL}}/user/delete/3"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
